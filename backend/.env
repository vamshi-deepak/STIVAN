# Backend environment configuration (dotenv format)
# Comments must start with #. Do not include JavaScript here.

# Google Gemini
GEMINI_API_KEY="set your API Key Here and change the model based on Your API"
GEMINI_MODEL=gemini-2.0-flash

# Server
PORT=5051
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/stivan

# Auth
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Security/limits
MAX_LOGIN_ATTEMPTS=5
# Lock time in milliseconds (default 2 hours). You can set ACCOUNT_LOCK_TIME_MINUTES instead for readability.
ACCOUNT_LOCK_TIME=7200000
ACCOUNT_LOCK_TIME_MINUTES=120
# OTP and email (optional)
OTP_EXPIRES_MINUTES=30
# SMTP settings (optional) - if not set the mailer will log OTPs to console
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=stivanhelp@gmail.com
SMTP_PASS=qgvfosictggrclaz
FROM_EMAIL=stivanhelp@gmail.com

# Admin bootstrap
ADMIN_EMAIL=admin@stivan.com
ADMIN_PASSWORD=Admin123!@
ADMIN_NAME=System Administrator
